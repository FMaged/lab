# powershell/

One Bootstrap script per guest — identity, AD DS promotion, domain join, GPOs —
plus the read-only `Test-SILab.ps1`. Conventions: the `powershell-provisioning`
skill; style: the global `powershell` skill; spec: `docs/ad-design.md`.
