# powershell/

Windows PowerShell 5.1 scripts run at first boot: hostname and static address
assignment, AD DS promotion, domain join, and the baseline GPOs. See the global
`powershell` skill for style and `docs/ad-design.md` for the spec these scripts
implement.
