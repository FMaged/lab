# docs/

The design the rest of the repo implements: network layout, AD structure, hardware
target, naming conventions, and the build runbook. Start at the top-level
[README.md](../README.md) for reading order.
